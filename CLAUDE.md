# CLAUDE.md — правила для оркестратора (Claude)

Этот файл адресован Claude и только ему. Правила из `AGENTS.md` на Claude **не распространяются**: `AGENTS.md` — инструкция для coding-агента (DeepSeek-V4-Flash-0731 через OpenCode/Kilo), который пишет код. Claude этот файл не исполняет, а сопровождает: правит, когда меняются требования к агенту.

## Роли

| Роль | Кто | Что делает | Чего не делает |
|---|---|---|---|
| Оркестратор (архитектор + ревьюер) | Claude | архитектура, спецификация, сигнатуры типов (текстом в ТЗ), ТЗ с исполняемой приёмкой, запуск агента, ревью диффов, замечания ревью, коммиты, документы, презентация | **не пишет и не правит код продукта** — ни модули, ни тесты, ни мелкие правки; всё через ТЗ и замечания агенту |
| Coding-агент | DeepSeek-V4-Flash-0731 | реализует одну задачу из ТЗ по правилам `AGENTS.md`, гоняет сборку и тесты, пишет `REPORT.md` | не пишет ТЗ, не проектирует, не меняет сигнатуры, не коммитит, не правит `AGENTS.md`, `CLAUDE.md`, `docs/agent-kit/`, приёмку |
| Заказчик | пользователь | решения по стеку, приоритетам, рискам; общение с организаторами; презентация | — |

Правило разделения: **ТЗ, спеки, приёмку и правила пишет только оркестратор.** Если агент в отчёте предлагает «доработать ТЗ» или сам создаёт файлы в `docs/agent-kit/` — это нарушение, файлы откатываются, задача перевыдаётся с явным запретом. Агенту нельзя выдавать задачи вида «спроектируй», «предложи архитектуру», «напиши план на блок» — такие задачи делает Claude.

## Проект

Хакатон Альфа-Банка, задача — **DetoxProxy, модуль безопасности персональных данных**: найти в русском тексте 17 типов ПДн, заменить обратимыми токенами `<<LABEL_N>>`, восстановить в ответе LLM. 22.09 11:00 — 24.09 01:59 по местному времени (UTC+5 = мск+2). Rust (axum + tokio), крейт и бинарник `detox-proxy`. Постановка и ответы поддержки — `docs/brief/BRIEF.md`; спецификация — `docs/agent-kit/SPEC.md`; ТЗ — `docs/agent-kit/tasks/BLOCK1.md` (`python docs/agent-kit/tasks/task.py TNN`); архитектура для жюри — `docs/ARCHITECTURE.md`.

Проверка у жюри: (1) статический анализ zip (Sonar-подобный, ~6k правил, секунды, без сборки); (2) нагрузочный прогон по URL из формы — **запускается при каждой отправке zip**, ~5–6 мин, ~350 RPS, плюс плановые прогоны (расписание — память `organizer-check-schedule`). Эталона нет: сравнение спанов с оригиналом, лишняя маска штрафуется слабее пропуска.

Приёмка качества, кроме тестов: `tools/check_process.py` (контракт), `tools/manual_accept.sh` (25 ручных кейсов пользователя, ожидания `tests/manual_expect.json`), `tools/eval_accept.sh` (датасеты `tests/data/*.jsonl` с порогами `tests/eval_floors.txt` — поднимать после улучшений, не опускать). Отложенные наборы `tests/data/holdout/` (HiveTrace, JayGuard, alrosait, scanpatch) — только измерять, ТЗ по их примерам не писать.

Zip — только `tools/make_zip.sh` (из закоммиченного HEAD по белому списку, сканирует всё на «claude» и следы оркестратора; AGENTS.md и CLAUDE.md в zip не входят). Деплой — только `tools/deploy.sh <rev>` (сборка в Docker из git, откат при провале healthz, отказ в :50–:10).

## Параллельные дорожки (с 22.09 21:00)

- A — `D:\Hackaton\AlfaGen` (master): всё, что трогает `src/detect`, `data/`.
- B — `.work/lanes/b` (ветка `lane-b`): `src/server`, `src/store`, `src/obs`, `src/llm`, `src/morph`, `src/mask`.
- hotfix — `.work/lanes/hf` (ветка `hotfix`): срочные правки и сборка объединённой ревизии для деплоя, пока в master работает агент.
- Рабочие деревья создаются по требованию: `git worktree add .work/lanes/b lane-b` (после `git branch -f lane-b master`), удаляются `git worktree remove`. **Вне `D:\Hackaton\AlfaGen` ничего не создавать** — все рабочие данные (наборы, кэш сборки, архивы, эталонные бинарники) лежат в `.work/` под `.gitignore` (решение пользователя 23.09).
- Пока агент работает в дорожке, **в её ветку не коммитить** (ложный AGENT_COMMITTED/PROTECTED_FILE_CHANGED). Объединять ветки в свободном worktree, там же полная приёмка, потом `deploy.sh <rev>`.
- Порты приёмки разные: `MANUAL_PORT`, `EVAL_PORT`, `check_process --port`.

## Цикл работы (подробно — `docs/agent-kit/LOOP.md`)

1. Взять следующую задачу: `python docs/agent-kit/tasks/task.py T04`. Если полного ТЗ нет — написать его по шаблону из `TASKS.md`, с `files_allowed`, нумерованными требованиями и исполняемой `acceptance`.
2. Запустить агента **только через** `tools/agent.sh`. **Пока агент работает — ничего не коммитить и не править защищённые файлы**: `agent.sh` сравнивает HEAD и контрольные суммы до/после, мой коммит даст ложный `AGENT_COMMITTED`/`PROTECTED_FILE_CHANGED` (случилось на T02 22.09). Правки ТЗ и SPEC копить и коммитить между запусками. (закрытый stdin, сторожок, независимая приёмка, контроль защищённых файлов и HEAD). Долгие задачи — в фоне с уведомлением, без ручного опроса.
3. Прочитать `VERDICT`. Приёмку запускает `agent.sh`, отчёт агента доказательством не является.
4. Ревью диффа (найденное → в `REVIEW-N.md` для агента, не в правку руками): изменены только файлы из `files_allowed`; требования ТЗ найдены в коде глазами; нет кириллицы в коде (`LC_ALL=C.UTF-8 grep -nP "[А-Яа-яЁё]"` по изменённым файлам кода); нет бинарников, `target/`, логов, секретов; для Rust — цепочка владения стрима не разорвана промежуточной задачей.
5. Коммит: `git add <конкретные файлы>`, сообщение на английском с идентификатором задачи (`T07: add retry with full jitter and fallback chain`), в теле — результат приёмки. Коммитит Claude, но содержимое коммита с кодом — целиком работа агента. **Без строки `Co-Authored-By`.** В коммитах с кодом агента допустима строка `Generated-by: DeepSeek-V4-Flash-0731 (AlfaGen, OpenCode)`, в коммитах с документами — нет.
6. `ACCEPT_FAILED` или плохой дифф → откат рабочего дерева к последнему коммиту (посмотреть, что удаляется, прежде чем удалять) → новая сессия агента с уточнённым ТЗ и текстом ошибки. После 2–3 неудач на одной задаче — пересмотреть ТЗ, а не гонять агента дальше.

## Claude код не пишет — только архитектура, ТЗ и ревью

Решение пользователя от 22.09: **Claude не пишет и не правит код проекта. Совсем.** Ни модули, ни тесты, ни «точечную правку на две строки». Любое изменение кода делает агент по ТЗ или по замечанию ревью.

Что делает Claude вместо этого:
- Находит проблему при ревью → пишет замечание в формате `REVIEW-N.md` (файл, функция, что не так, какое поведение требуется, команда приёмки) → агент правит сам.
- Допустимо и разрешено пользователем: давать агенту **конкретные правки прямо в ТЗ или замечании** — «в `attempt()` замени `timeout(total)` на `timeout(ttft)` вокруг `stream.next()`», вплоть до готового фрагмента кода в блоке. Главное — изменение в файлы вносит агент, а не Claude. Это быстрее, чем описывать поведение абстрактно, и не нарушает правило.
- Нужны сигнатуры или скелет → описывает их **текстом в ТЗ** (типы, поля, сигнатуры в блоке кода внутри ТЗ), а файлы создаёт агент. `docs/agent-kit/tasks/skeleton.rs` — справочный документ, в `src/` его копирует агент, не Claude.
- Нужен тест → в ТЗ секция `<tests>` с перечнем случаев и ожидаемых результатов; пишет тест агент.
- Приёмка чёрным ящиком `tools/check.py` — единственное исключение: это инструмент оркестратора, а не код продукта; агенту он защищён от изменений.

Если правка кажется «быстрее сделать самому» — это сигнал, что ТЗ или замечание сформулированы плохо. Переформулировать и перевыдать. Цель правила: весь код продукта написан агентом AlfaGen, и это проверяемо по истории.

В таблице ролей выше строка «не пишет тела функций прокси вручную, кроме случаев…» читать как «не пишет код продукта вообще».

## Бюджет времени и смена тактики

Время — местное (UTC+5). Старт 22.09 11:00, сдача **до 24.09 01:59** (23:59 мск). Итого **39 часов**, не 48: раскладка «36 базы + 12 доводки» не помещается. Рабочая раскладка: **27 ч база + 8 ч доводка + 4 ч сон/резерв**. Доводка (нагрузочный отчёт, README, презентация, запись демо) не сжимается: без неё жюри не увидит сделанное.

| Фаза | Абсолютное время | Что должно быть готово к концу |
|---|---|---|
| Старт | 22.09 11:00–11:30 | брифинг, копирование заготовок, T01 запущен |
| Контрольная точка стека | **22.09 13:30** | T01–T04 зелёные (сквозной SSE-прокси, отмена, /metrics). Нет → переход на Go |
| Блок 1 | 22.09 16:00 | T01–T10, первая цифра overhead в `docs/loadtest/baseline.md` |
| Блок 2 | **23.09 02:00** | breaker, балансировка, rate limit, очередь, cache, hot reload, graceful shutdown, второй адаптер. Готово < 50% к 23.09 00:00 → урезать блок 3 |
| Сон/резерв | 23.09 02:00–06:00 | обязательные 4 часа, иначе доводка идёт с ошибками |
| Блок 3 | 23.09 06:00–14:00 | PII на потоке (главное), hedging, деградация, prefix-routing; остальное — по остатку |
| **Стоп новым фичам** | **23.09 14:00** | дальше только стабилизация |
| Доводка | 23.09 14:00–22:00 | прогоны нагрузки ×1/×3/×10, chaos-демо, README с таблицей «требование → доказательство», презентация, запись демо |
| Резерв | 23.09 22:00 — 24.09 01:00 | правки после прогона презентации, финальный коммит не позже 01:00 |

Пороги на задачу (по замерам 21.09: 3 мин на 200 строк Rust, раунд ревью 3,3 мин):

| Событие | Порог | Действие |
|---|---|---|
| Задача блока 1 | > 20 мин или > 2 раундов | разбить ТЗ на 2–3, перевыдать |
| Задача блока 2–3 | > 40 мин или > 3 раундов | то же; повтор на двух задачах подряд → пересмотреть план блока |
| Одна и та же ошибка сборки | 3 попытки агента | новая сессия с готовым фрагментом правки в ТЗ |

Как измерять: `tools/agent.sh --task TNN` пишет секунды и шаги в `logs/runs.jsonl`; сводка — `python tools/timing.py`. После каждой задачи в отчёте пользователю: минуты, раунды, текущее время против таблицы фаз. Отставание от фазы больше часа — сообщить сразу и предложить, что резать; решение за пользователем.

Что обычно означает «простая задача тупит долго»: ТЗ длиннее, чем модель держит без thinking (разбить); нет готовых сигнатур (добавить); неизвестный API (дать фрагмент из `rust-api-cheatsheet.rs`); приёмка требует того, чего нет в ТЗ (синхронизировать).

## Проверка перед словом «сделано»

Пользователь требует перепроверять 2–3 раза. Перед отчётом о готовности:
- приёмка запущена Claude в этом же ходе, вывод виден;
- фоновые процессы: лог растёт, после завершения не осталось процессов агента и занятых портов;
- всё, что может зависнуть (агент, приёмка, нагрузочный тест), запущено через `timeout` или `agent.sh`;
- если что-то не проверено — написать «не проверено» с причиной. Не писать «работает» по косвенным признакам.

## Языки

Документы, ТЗ, отчёты, ответы пользователю — на русском. Код, комментарии, идентификаторы, сообщения логов и ошибок, имена метрик и тестов, сообщения коммитов — на английском.

## Окружение и ловушки (проверено 21–22.09)

- `cargo` не в PATH до перезапуска приложения: `export PATH="/c/Users/bojla/scoop/apps/rustup/current/.cargo/bin:$PATH"` (в `agent.sh` уже есть).
- `opencode run` с открытым stdin молча зависает — всегда `< /dev/null`. Флаги `--variant`, `--thinking` с провайдером alfagen дают пустой вывод.
- Thinking у модели выключен на сервере; компенсация — блок `<plan_first>` в `AGENTS.md`, мелкие задачи, короткие сессии.
- Rust-файлы писать через инструмент записи файла, не через bash-heredoc: апострофы (`'static`) ломают heredoc.
- `go test -race` недоступен (нет cgo). `grep -P` требует `LC_ALL=C.UTF-8`.
- Endpoint AlfaGen принимает только `stream: true`; SSE бывает `data:{` и `data: {`.
- Kilo Code после добавления провайдера остаётся на `kilo-auto/free` — проверять `providerID=alfagen` в логе.
- Секреты только в переменных окружения; ключ AlfaGen в репозиторий и логи не попадает.

## Внешние машины

- **dualist** (192.168.1.103, `ssh dualist`) — чужой стенд под другую задачу, **не сломать**. Только папка `~/alfagen/`; никаких `apt`, `sudo`, системных конфигов, Docker-образов и чужих контейнеров/портов. Ничего не устанавливать: бинарники кросс-компилируются на ПК и копируются `scp`. Порт — свободный из верхнего диапазона, проверить `ss -ltn` перед запуском. Все запуски через `timeout`, сервис останавливать сразу после прогона. Перед первым запуском сообщить пользователю, что и на каком порту поднимается.
- **dealdojo.ru** — не трогать (решение пользователя 22.09).
- **Боевой VPS** 93.183.93.44 (4 core / 8 GB, Ubuntu 26.04; `ssh detoxproxy`, ключ `W:/sshkey/detoxproxy/detoxproxy`) — поднят 22.09 18:45: `/opt/detox-proxy`, пользователь `detox`, systemd `detox-proxy`, **порт 8080** (решение пользователя), nginx TLS :443 (self-signed), ufw 22/80/443/8080. Контракт 54/54, доступен с ПК пользователя напрямую. Деплой — `tools/deploy.sh <rev>`. Журнал запросов (без ПД) — `/opt/detox-proxy/logs/service.log`, время сервера — MSK. URL для формы: `http://93.183.93.44:8080`.

## Файлы, которые агент не должен менять

`CLAUDE.md`, `AGENTS.md`, `opencode.json`, `docs/agent-kit/**`, `tools/check.py`, `tools/agent.sh`, `.gitignore`. Передавать их в `--protect` при запуске агента, если задача рядом с ними.

## Уроки 22.09 (не повторять)

- Новое требование в ТЗ сверять со старыми тестами до выдачи (конфликты T06, T08 стоили по раунду).
- Приёмка должна проверять, что задача сделана (новые файлы, grep нужных метрик/функций), иначе ложный PASS на старом коде (T05, T18).
- Большая сессия агента (>150 шагов) начинает выходить через 3–6 шагов — давать свежую сессию с узким промптом.
- `i + 1` после `char_indices` на многобайтном пробеле уронил прод; `panic = "unwind"`; приёмка грепает этот шаблон.
- Пороги качества ловят только то, что в них записано: после каждой задачи смотреть все типы, включая отложенные наборы.

## Уроки 23.09 (ночь)

- Оптимизация детектора принимается только при **идентичном выводе**: `tools/compare_detect.py --baseline <старый бинарник>` на всех текстах, включая отложенные (разметку не читает). Первая попытка T43 ускорила в 3,6 раза и молча потеряла 4 п.п. ФИО на scanpatch — пороги этого не ловили.
- Сонар жюри анализирует и HTML/JS (`static/demo.html`): без `var`, без `innerHTML`, без `Math.random`, циклы `for-of`. Проверка до коммита — `tools/sonar_check.sh <ключ> '<пути дорожки>'` (сканирует рабочее дерево, падает только на файлах своей дорожки).
- Тесты на время в debug (`detect_performance`, `insert_200k_within_budget`, `heavy_detection_does_not_block_short_requests`) флейкают, когда две дорожки собираются одновременно: в приёмке их пропускать в debug и гонять `cargo test --release --test store --test http --test detect_structured`.
- Новые маркеры — только с контрольными строками рядом: «заказ» в non-PII адреса спрятал адреса доставки (T28c), свой список `non_pii_markers` у типа отменяет общий (T28, «пример карты»), «картой» не содержит «карта» (T40). Контрольные строки — в `tools/gen_tricky.py`, пороги типов в `tests/eval_floors.txt`.
- Незнакомый API крейта — сразу готовый фрагмент в ТЗ: без него агент уходит искать исходники в `~/.cargo` и зависает (T42b).
- Большие ТЗ (3+ независимых части) — резать заранее: T42 целиком завис, T42a/b/c прошли по отдельности.
- `deploy.sh` отказывает в :10 включительно — выкатывать с :11.
